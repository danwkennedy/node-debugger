/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {Debugger} from './debugger';

describe('Debugger', () => {
  it('should create an instance', () => {
    expect(new Debugger()).toBeTruthy();
  });
});
